/*
 * черновик приложения, которое является комбинацией редактора и просмотрщика объектов внутри сферы
 * это как веб браузер, только для сферы.
 * По сути это попытка изъять из сферы подобную функциональность и вынести её в отдельное приложение потому,
 * что сфера это сеть, это среда, фреймворк, а не приложение.  
 */

exports.init = function(env, dsa){
    dsa.on('create', function(sprout, stack){
	       
	   });
    dsa.on('destroy', function(sprout, stack){
	   });
}